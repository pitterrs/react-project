import React from "react";
import MaterialAccordionSummary from '@material-ui/core/AccordionSummary';
import classNames from 'classnames';
import styles from './Accordion.module.scss';

export const AccordionSummary = ({ className, children, ...props }) => {
  return (
    <MaterialAccordionSummary className={classNames(styles.accordionSummary, className)} {...props}>{children}</MaterialAccordionSummary>
  );
};
