import React from "react";
import MaterialCardActions from '@material-ui/core/CardActions';

export const CardActions = ({ children, ...props }) => {
    return (
        <MaterialCardActions {...props}>{children}</MaterialCardActions>
    );
};
