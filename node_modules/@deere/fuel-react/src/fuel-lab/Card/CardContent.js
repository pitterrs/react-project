import React from "react";
import MaterialCardContent from '@material-ui/core/CardContent';

export const CardContent = ({ children, ...props }) => {
    return (
        <MaterialCardContent {...props}>{children}</MaterialCardContent>
    );
};
