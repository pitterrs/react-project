import React from "react";
import MaterialCardMedia from '@material-ui/core/CardMedia';

export const CardMedia = ({ children, ...props }) => {
    return (
        <MaterialCardMedia {...props}>{children}</MaterialCardMedia>
    );
};
