import React from "react";
import MaterialDialogContentText from '@material-ui/core/DialogContentText';

export const DialogContentText = ({ children, ...props }) => {
    return (
        <MaterialDialogContentText {...props}>
            {children}
        </MaterialDialogContentText>
    );
};
