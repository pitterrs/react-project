import React from "react";
import MaterialTooltip from '@material-ui/core/Tooltip';

export const Tooltip = React.forwardRef(({ ...props }, ref) => {
    return (
        <MaterialTooltip ref={ref} {...props} />
    );
});
